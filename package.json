{
  "name": "ai-organization-economics-calculator",
  "version": "0.3.0",
  "description": "Local-first research calculator for AI productivity, enterprise economics, and workforce scenarios.",
  "license": "MIT",
  "private": false,
  "scripts": {
    "start": "python3 -m http.server 8000",
    "test": "node --test *.test.js",
    "check": "node --check calculator.js && node --check locale-packs.js && node --check i18n.js && node --check app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
